# Labyrinth Algorithm in JS

This is JavaScript based implementation of the algorithms covered in our DSAI lessons.

## Usage
### Start
run `npm run dev` to start the development website

### Controls
- any digit run the corresponding algorithm
- `r` to regenerate the labyrinth
- `p` to temporarily pause the algorithm
- `f` to toggle simulation